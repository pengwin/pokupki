machine:
  node:
    version: 8.1.3
test:
  override:
    - npm run test
    - if [[ -e test-results.xml ]]; then cp test_artifacts/test-results.xml $CIRCLE_TEST_REPORTS/test-results.xml; fi

deployment:
  staging:
    branch: master
    heroku:
      appname: lit-brushlands-10843
